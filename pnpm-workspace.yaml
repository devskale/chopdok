# pnpm 11 reads build-script policy from here (not package.json).
# canvas = pdf.js's optional Node-canvas dep (unused — this app renders in the browser).
# unrs-resolver = eslint-import-resolver's native binary (not needed for test/build).
onlyBuiltDependencies: []
ignoredBuiltDependencies:
  - canvas
  - unrs-resolver
